#include "main.h"
#include <unistd.h>

/**
 * _puts - function that prints a string
 * @str: string value
 * Return: null
 */

void _puts(char *str)
{
	while (*str != '\0')
	{
		_putchar(*str);
		str++;
	}
	
========== 3-puts ==========
3-puts:0: ERROR: Does not appear to be a unified-diff format patch
total: 1 errors, 0 warnings, 0 lines checked
_putchar('\n');
}
