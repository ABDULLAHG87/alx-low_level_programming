#include "main.h"

/**
 * _isalpha - Function to Detection Alphabet
 * @c: Parameters to be tested
 * Return: 0 (Success)
 */
int _isalpha(int c)
{
	int flag;

	if ((c >= 97 && c <= 122) || (c >= 65 && <= 90))
		flag = 1;
	else
		flag = 0;
	return (flag);
}
