#include "main.h"

/**
 * print_number - function that prints integer
 * @n: value
 * Return: 0 (Success)
 */

void print_number(int n)
{
	if (n < 0)
	{
		putchar ('-');
		n = -n;
	}
	if (n / 10)
		print(n / 10);
	putchar('0' + n % 10);
}
