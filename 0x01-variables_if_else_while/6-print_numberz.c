#include <stdio.h>

/**
 * main - Entry Point
 * Task 6: Numberz
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number;

	for (number = 48; number <= 57; number++)
		putchar(number);
	putchar('\n');
	return (0);
}
